package com.maputo.maputo_tour.repository;

import com.maputo.maputo_tour.model.RotaTur;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RotaTurRepository extends JpaRepository<RotaTur, Long> {
}
